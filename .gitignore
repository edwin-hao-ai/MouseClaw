# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Tauri build artifacts
src-tauri/target/
src-tauri/gen/
target/

# Runtime / Generated data
ruvector.db
.mouseclaw/
*.tsbuildinfo

# Whisper models (downloaded at runtime, ~142MB)
models/
*.bin
*.gguf

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
# MDDock local metadata (do not commit)
.mddock/
entities/
mddock.md
*.dmg
/marketing/
# (mp4/gif intentionally committed for README; docs/marketing/ is tracked too)

# Awareness daemon's local state (gitignored, machine-local)
.awareness/

# Whisper model bundled into the .app at build time (fetched by scripts/fetch-whisper-model.sh)
src-tauri/resources/ggml-*.bin
# Sherpa ASR + punctuation ONNX models — fetched by scripts/fetch-sherpa-model.sh at build time,
# too big for git (~180MB ASR + ~72MB punct)
src-tauri/resources/sherpa-zh-en/
src-tauri/resources/sherpa-punct/
.claude/scheduled_tasks.lock
